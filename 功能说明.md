# BOM Tools 功能说明

## 1. 项目定位

BOM Tools 是面向硬件研发、BOM 审核、物料选型和 PLM 上传流程的内部辅助平台。项目以 Flask Web 应用为主入口，把多个 Excel 处理、飞书表格匹配、BOM 比对、PLM 格式转换、问题反馈和需求工单能力集中在一个页面中，便于多人在局域网或单机环境下使用。

平台核心目标：

- 减少 BOM 格式整理、供应商展开、PLM 上传文件制作等重复人工操作。
- 支持从飞书优选库、关系库中批量匹配物料信息。
- 支持 BOM 版本差异、客户 BOM 与 HQ 标准格式差异的快速定位。
- 提供 Bug 和需求反馈入口，便于工具持续迭代。
- 支持 Windows 离线部署，适配无外网办公环境。

## 2. 系统组成

项目主要目录如下：

| 目录 / 文件 | 说明 |
| --- | --- |
| `web_app2/app.py` | Web 应用入口，注册各功能模块 Blueprint，提供首页和下载接口 |
| `web_app2/templates/index.html` | 单页前端界面，包含所有工具的页面模板和交互脚本 |
| `web_app2/shared.py` | 公共工具方法、上传目录、输出目录、缓存目录、Excel 读写辅助方法 |
| `web_app2/bom/` | BOM 格式转换模块 |
| `web_app2/feishu/` | 飞书表格读取、缓存、匹配和优选率查询模块 |
| `web_app2/plm/` | PLM 上传格式转换、规格型号提取、PLM 自动化辅助模块 |
| `web_app2/bom_compare/` | BOM 比对工具集合 |
| `web_app2/manufacturer_alias/` | 厂商规范名和别名映射管理模块 |
| `web_app2/bug_report/` | Bug 提交、查询、状态管理模块 |
| `web_app2/feature_request/` | 需求工单提交、点赞、查询、状态管理模块 |
| `deploy_bundle/` | Windows 离线部署包，包含依赖 wheel 和一键安装启动脚本 |
| `scripts/` | 早期或独立使用的命令行脚本 |
| `tests/` | 自动化测试用例 |

## 3. 运行方式

### 3.1 本地有网环境

进入 Web 应用目录后安装依赖并启动：

```bash
cd web_app2
pip install -r requirements.txt
python app.py
```

默认访问地址：

```text
http://localhost:5000
```

服务默认监听 `0.0.0.0:5000`，也可以通过环境变量 `PORT` 指定端口。

### 3.2 Windows 离线部署

将 `deploy_bundle/` 整个目录复制到目标电脑，运行：

```text
deploy_bundle/install_and_run.bat
```

离线部署脚本会创建虚拟环境、安装内置 wheel 依赖并启动服务。

### 3.3 文件目录规则

运行过程中会使用以下目录：

| 目录 | 用途 |
| --- | --- |
| `web_app2/uploads/` | 临时保存用户上传的 Excel 文件 |
| `web_app2/outputs/` | 保存转换、匹配、比对后生成的下载文件 |
| `web_app2/cache/` | 保存飞书表格服务端缓存 |

上传文件和输出文件会被后台定时清理。飞书缓存文件不会自动清理，通常由用户在界面中手动刷新或清除。

## 4. 支持的文件类型

主要处理 Excel 文件：

- 支持 `.xlsx`、`.xlsm`
- 部分模块前端允许选择 `.xls`
- 服务端对旧版 `.xls` 通常会提示另存为 `.xlsx`
- 如果 Excel 文件被公司加解密软件锁定或实际不是标准 Excel 包，系统会返回无法读取文件的提示

附件类功能还支持图片、CSV、日志文本、压缩包、Office/PDF 等，具体以 Bug 或需求提交页面的文件选择限制为准。

## 5. 功能总览

| 功能模块 | 主要用途 |
| --- | --- |
| BOM 格式转换 | 将客户 BOM 中多供应商、多型号信息展开为标准多行格式，或转换为 HQ 整机 BOM 配置表 |
| 飞书优选库 + 关系库匹配 | 根据本地 BOM 的关键列到飞书在线表格中匹配 HQ 料号、规格型号、制造商、优选等级等信息 |
| 查询 BOM 优选率 | 根据 BOM 中的 HQ 料号查询已缓存优选库，统计优选料比例 |
| 转换为上传 PLM 系统格式 | 将整机 BOM 配置表转换为 PLM 导入格式，也支持规格型号提取 |
| PLM 自动化辅助 | 使用账号密码登录 PLM，进行规格反查导出或 HQ 料号附件下载 |
| BOM 比对工具合集 | 支持客户 BOM 与 HQ 标准 BOM、通用 BOM、HQ 版本、整机 HQ 版本、Cadence 与 HQ 的差异比对 |
| 厂商命名映射 | 维护厂商规范名与别名，用于匹配时统一厂家名称 |
| 小工具合集 | 浏览器本地计算文件哈希值，不上传服务器 |
| Bug 提交 | 提交问题、附件、复现步骤，并维护处理状态 |
| 需求工单 | 提交功能需求、点赞、筛选、排序并维护状态 |

## 6. BOM 格式转换

### 6.1 使用场景

用于处理客户 BOM 中供应商、品牌、厂家型号混在一起的情况，将一行物料的多供应商拆成多行，或者整理成 HQ 内部整机 BOM 配置表。

典型场景：

- 客户 BOM 中一列同时包含多个品牌和型号。
- 品牌列与型号列分开，但每列内部使用分号分隔多个供应商。
- 制造商字段中包含内部编号、冒号、括号等特殊格式。
- 需要将客户原始 BOM 整理成便于审核的标准展开格式。

### 6.2 支持的输入格式

系统支持三类供应商解析方式：

| 格式 | 说明 |
| --- | --- |
| 格式 A | 品牌和型号合并在同一列，例如使用 `品牌:型号`、`||`、空格等方式分隔 |
| 格式 B | 品牌列和型号列分开，多个品牌或型号用分号分隔 |
| 格式 C | 制造商列包含内部编号或特殊编码，型号列使用冒号分隔 |

### 6.3 操作流程

1. 上传 BOM Excel 文件。
2. 选择 Sheet 和表头行。
3. 点击刷新预览，系统自动识别列角色。
4. 检查或手动填写以下列：
   - 物料名称列
   - 用量列
   - 品牌 / 厂家列
   - 型号列
5. 选择输出模式。
6. 点击开始转换并下载结果文件。

### 6.4 输出模式

| 输出模式 | 结果 |
| --- | --- |
| 原格式展开 | 保留原始 BOM 主要字段，将多供应商拆为多行，增加供应商、型号、是否独供等字段 |
| HQ 整机 BOM 配置表 | 输出 HQ 内部评审或配置使用的整机 BOM 表格格式 |

### 6.5 处理规则

- 空行会跳过。
- 多供应商会展开为多行。
- 第一供应商保留原始行的主要信息和用量。
- 后续供应商行主要填供应商和型号，避免重复写入整行数据。
- 数字用量会尽量保留为整数或浮点数。

## 7. 飞书优选库 + 关系库匹配

### 7.1 使用场景

用于将本地 BOM 中的规格型号、制造商、客户料号等字段，与飞书内部表格中的优选库或关系库进行批量匹配，输出 HQ 料号、HQ 规格型号、HQ 制造商、优选等级、描述等字段。

### 7.2 网关设置

界面包含以下网关配置：

| 字段 | 说明 |
| --- | --- |
| 网关地址 | 默认使用内部网关地址 |
| Origin | 内部 API 调用所需 Origin |
| 工号 | 当前用户工号 |

网关地址会经过白名单校验，避免配置到非预设地址。

### 7.3 表格库配置

系统内置多张飞书表格库配置，包括优选库和客户物料型号与 HQ 料号关系库。用户可以在界面中：

- 连接飞书表格并获取 Sheet 列表。
- 查看每个 Sheet 的表头。
- 启用或禁用某个 Sheet。
- 配置本地匹配键和飞书匹配键。
- 配置需要提取输出的飞书字段。
- 一键缓存所有启用 Sheet。
- 导出当前配置。
- 恢复默认配置。
- 清除本地配置。

### 7.4 匹配规则

支持多键匹配。每个启用 Sheet 可以配置多组键：

- 本地键：来自上传 BOM 的列名。
- 飞书键：来自飞书表格的列名。
- 键数量必须一致。
- 所有键组合后作为 AND 条件匹配。

例如：

| 本地键 | 飞书键 |
| --- | --- |
| 规格型号 | HQ 规格型号 |
| 厂商 | HQ 制造商 |

只有两个字段都匹配时，才认为该行命中。

### 7.5 厂商映射

本地键可以启用厂商映射。启用后，系统会先在厂商命名映射库中查找输入厂商名，将其转换为 HQ 规范名，再参与匹配。

这适用于以下情况：

- 客户写法与 HQ 规范写法不一致。
- 同一厂商有中文、英文、简称、大小写、空格等多种写法。
- 需要统一后再与优选库字段匹配。

### 7.6 缓存机制

飞书表格数据可以缓存到服务端：

- 缓存粒度为 `token + sheet_id`。
- 缓存文件保存在 `web_app2/cache/`。
- 匹配时优先使用缓存。
- 如果缓存不存在或失效，系统会尝试实时读取飞书表格。
- 飞书缓存不会被后台自动删除。

缓存适合大表或多人重复使用的场景，可显著减少接口读取耗时。

### 7.7 输出结果

输出 Excel 包含：

- 本地 BOM 原始列。
- 配置提取的飞书字段。
- 来源表格。

未匹配行会保留原始数据，并标记为未匹配。

当同一行在多个表中命中时，系统会根据来源优先级处理。关系库来源优先级高于普通优选库来源。

## 8. 查询 BOM 优选率

### 8.1 使用场景

用于统计一个 BOM 中已有 HQ 料号的优选情况，快速评估 BOM 的优选料比例。

### 8.2 前置条件

需要先在飞书优选库模块中缓存相关优选库 Sheet。优选率查询使用缓存数据，不会直接重新拉取所有飞书表格。

### 8.3 操作流程

1. 上传包含 HQ 料号的 BOM 文件。
2. 选择 Sheet 和表头行。
3. 选择 HQ 料号列。
4. 点击开始查询。
5. 下载查询结果。

### 8.4 优选判定规则

系统根据优选等级字段判断是否为优选料：

| 优选等级值 | 判定 |
| --- | --- |
| 包含“优选”字样 | 优选 |
| 包含 `preferred` | 优选 |
| 数字大于等于 7 | 优选 |
| 包含非优选、not preferred 等否定描述 | 非优选 |
| 数字小于 7 或其他值 | 非优选 |

### 8.5 统计口径

优选率计算公式：

```text
优选率 = 优选料数量 / 已匹配料数量
```

未匹配料不参与优选率分母。

输出结果会对不同状态进行颜色标注：

| 状态 | 颜色含义 |
| --- | --- |
| 优选 | 绿色 |
| 匹配到但非优选 | 黄色 |
| 未匹配 | 灰色 |

## 9. 转换为上传 PLM 系统格式

### 9.1 整机 BOM 配置表转换

该功能用于将整机 BOM 配置表转换为 PLM 系统标准导入格式。

#### 输入内容

- 整机 BOM 配置表 Excel。
- Sheet 名称。
- 表头行。
- 序号列。
- HQ PN 列。
- 主二供列。
- 用量配置列。

#### 操作流程

1. 上传整机 BOM 配置表。
2. 选择 Sheet 和表头行。
3. 点击加载 / 刷新。
4. 检查系统自动识别的基础列。
5. 配置一个或多个用量列。
6. 可从表头上一行或表头行读取配置名称。
7. 点击开始转换。
8. 下载 PLM 导入文件。

#### 输出结果

输出为 PLM 可导入的 Excel 文件，通常包含序号、料号、单耗、替代关系、主辅 BOM 标记等字段。

### 9.2 规格型号提取

该功能用于从 BOM 中抽取某一列的所有值，生成单列 Excel，列名为“规格型号”。

#### 适用场景

- 将客户 BOM 中的规格型号整理出来做批量查询。
- 将某个字段去重、清洗后提供给后续 PLM 或飞书查询流程。

#### 操作流程

1. 上传 BOM 文件。
2. 选择 Sheet 和表头行。
3. 选择需要提取的列。
4. 可选：选择剔除列。
5. 点击开始提取。
6. 下载规格型号列表。

## 10. PLM 自动化辅助

该模块通过浏览器自动化方式辅助访问 PLM 系统。

### 10.1 规格反查导出

输入：

- PLM 账号。
- PLM 密码。
- 待查询 Excel 文件。

功能：

- 自动登录 PLM。
- 根据上传文件中的规格型号或相关信息进行查询。
- 导出查询结果。

### 10.2 HQ 料号附件下载

输入：

- PLM 账号。
- PLM 密码。
- HQ 料号。

功能：

- 自动登录 PLM。
- 搜索指定 HQ 料号。
- 打开物料详情。
- 下载选中的附件。
- 输出附件压缩包或下载结果文件。

### 10.3 注意事项

- 自动化依赖浏览器环境和 PLM 页面结构。
- 如果 PLM 页面改版，可能需要调整自动化脚本。
- 账号密码仅用于当前自动化请求，不应写入文档或提交到仓库。

## 11. BOM 比对工具合集

BOM 比对模块用于快速识别两个 BOM 或两个版本之间的新增、删除、字段变化、位号变化等差异。

### 11.1 客户 BOM 与 HQ BOM 预览 / 导出

适用于将客户 BOM 与 HQ 标准 BOM 进行对齐检查。

功能点：

- 上传客户 BOM 和 HQ BOM。
- 选择各自 Sheet 与表头行。
- 配置匹配键。
- 配置字段映射。
- 先预览匹配情况。
- 再导出标准化对比结果。

### 11.2 通用 BOM 比对

适用于任意两个 Excel 表之间的字段级比对。

功能点：

- 自动读取两个文件的 Sheet 和表头。
- 支持选择匹配键。
- 支持选择同名字段或手动添加字段映射。
- 支持输出差异报告。

### 11.3 HQ 版本比对

适用于两个 HQ BOM 版本之间的差异分析。

功能点：

- 上传基准版本和对比版本。
- 按料号或配置的关键字段匹配。
- 选择需要比较的字段。
- 输出新增、删除、变更、重复项等统计和明细。

### 11.4 整机 HQ 版本比对

适用于整机 BOM 配置表版本之间的比对。

功能点：

- 默认按料号识别同一物料。
- 可选择比较字段。
- 支持不同 Sheet 与表头行。
- 输出差异报告。

### 11.5 Cadence BOM 与 HQ BOM 比对

适用于设计输出的 Cadence BOM 与 HQ BOM 标准表之间的对照。

功能点：

- 上传 Cadence BOM 和 HQ BOM。
- 分别选择表头行。
- 配置两边匹配键。
- 选择同名字段或手动添加字段映射。
- 导出差异报告。

### 11.6 比对处理特性

比对逻辑包含以下处理：

- 支持自动识别常见关键列。
- 支持多字段映射。
- 支持数量字段的数字化比较。
- 支持位号字段拆分和变化识别。
- 支持重复键提示。
- 支持生成 Excel 差异报告。

## 12. 厂商命名映射

### 12.1 功能用途

厂商命名映射用于维护“HQ 规范厂商名”和“客户 / 表格中出现的厂商别名”之间的关系。

例如：

| HQ 规范名 | 可能的别名 |
| --- | --- |
| Murata | MURATA、村田、Murata Manufacturing |
| TDK | TDK Corporation、东电化 |

### 12.2 主要能力

- 查询厂商名或别名。
- 查看规范名、别名、归一化键、来源、备注。
- 新增别名映射。
- 删除别名映射。
- 在飞书匹配时作为本地键转换逻辑使用。

### 12.3 匹配价值

厂商名经常存在大小写、空格、中文英文、简称和全称差异。通过映射库预先归一化，可以提高飞书匹配准确率，减少因厂家写法不同导致的未匹配。

## 13. 小工具合集

当前小工具主要提供文件哈希计算。

### 13.1 文件哈希

功能：

- 支持选择多个文件。
- 在浏览器本地计算哈希。
- 显示计算进度。
- 文件不上传服务器。

适用场景：

- 校验文件是否一致。
- 校验离线部署包、Excel 输出文件或附件是否被修改。

## 14. Bug 提交

### 14.1 提交内容

Bug 提交页面支持填写：

- 姓名。
- 工号。
- 影响模块。
- 严重程度。
- 问题标题。
- 问题描述。
- 复现步骤。
- 期望结果。
- 附件。

附件支持截图、Excel、CSV、日志文本和压缩包等。

### 14.2 问题记录管理

问题记录支持：

- 按状态筛选。
- 按模块筛选。
- 按标题、描述、提交人搜索。
- 查看详情。
- 打开附件。
- 修改处理状态。

状态包括：

- 待处理
- 处理中
- 已修复
- 已关闭
- 暂缓
- 无法复现

## 15. 需求工单

### 15.1 提交内容

需求工单支持填写：

- 姓名。
- 工号。
- 需求类型。
- 优先级。
- 影响模块。
- 需求标题。
- 需求背景。
- 需求说明。
- 预期价值。
- 验收标准。
- 附件。

附件支持截图、Excel、CSV、Office/PDF、日志文本和压缩包等。

### 15.2 需求记录管理

需求记录支持：

- 按状态筛选。
- 按模块筛选。
- 按提交时间排序。
- 按点赞数排序。
- 搜索标题、说明、提交人。
- 查看详情。
- 打开附件。
- 点赞。
- 修改状态。

状态包括：

- 待评估
- 已纳入
- 开发中
- 已完成
- 暂缓
- 已关闭

## 16. 配置保存机制

飞书匹配相关配置主要保存在浏览器 `localStorage` 中，包括：

- 网关配置。
- 用户工号。
- 本地匹配键配置。
- 飞书表格 Token。
- Sheet 启用状态。
- 每个 Sheet 的匹配键。
- 提取字段映射。
- 缓存 key。

因此：

- 同一浏览器刷新页面后配置仍会保留。
- 换电脑、换浏览器或清除浏览器缓存后，本地配置可能丢失。
- 可以通过导出当前配置保存配置快照。
- 新用户可以使用恢复默认配置加载内置表格库。

## 17. 下载与清理机制

所有转换、匹配、比对结果通过统一下载接口提供：

```text
/download/<filename>
```

服务启动后会开启后台清理线程：

- 每 10 分钟执行一次。
- 清理上传目录中超过 30 分钟的临时文件。
- 清理输出目录中超过 30 分钟的结果文件。
- 不自动清理飞书缓存目录。

建议用户在生成结果后及时下载。

## 18. 安全与使用注意事项

- 本工具面向内部网络和内部数据场景，不建议直接暴露到公网。
- 上传文件会临时保存在服务器本地目录。
- 飞书匹配会调用内部 API 网关。
- PLM 自动化需要输入账号密码，应只在可信机器上运行。
- 结果文件会短时间保存在 `outputs/` 中，超过清理时间后可能无法下载。
- `.xls` 文件建议先在 Excel 中另存为 `.xlsx`。
- 如果公司加解密软件未正常运行，Excel 文件可能无法被服务端读取。

## 19. 典型业务流程

### 19.1 客户 BOM 整理到 HQ 审核

1. 使用 BOM 格式转换上传客户 BOM。
2. 自动识别并调整品牌、型号、用量列。
3. 输出原格式展开或 HQ 整机 BOM 配置表。
4. 如需补全 HQ 料号，进入飞书匹配模块。
5. 配置本地键和飞书键，执行匹配。
6. 下载匹配结果供审核。

### 19.2 BOM 优选率评估

1. 先在飞书匹配模块缓存优选库数据。
2. 上传包含 HQ 料号的 BOM。
3. 选择 HQ 料号列。
4. 执行优选率查询。
5. 下载带颜色标注的结果文件。

### 19.3 整机 BOM 上传 PLM

1. 准备 HQ 整机 BOM 配置表。
2. 进入 PLM 格式转换模块。
3. 选择 Sheet、表头行、序号列、HQ PN 列、主二供列。
4. 配置各机型或配置项用量列。
5. 转换并下载 PLM 导入文件。
6. 上传到 PLM 系统。

### 19.4 BOM 版本变更检查

1. 进入 BOM 比对工具合集。
2. 选择适合的比对类型。
3. 上传基准版本和对比版本。
4. 配置匹配键和比对字段。
5. 执行比对。
6. 下载差异报告，检查新增、删除和字段变化。

## 20. API 功能入口概览

| 接口 | 用途 |
| --- | --- |
| `POST /api/bom/detect` | BOM 文件 Sheet、表头、列角色识别和预览 |
| `POST /api/bom/convert` | BOM 格式转换 |
| `POST /api/feishu/sheets` | 获取飞书表格 Sheet 列表和表头 |
| `POST /api/feishu/load` | 读取并缓存飞书 Sheet 数据 |
| `POST /api/feishu/local_sheets` | 读取本地 Excel Sheet 和表头 |
| `POST /api/feishu/match` | 执行飞书多表匹配 |
| `POST /api/feishu/pref_rate` | 查询 BOM 优选率 |
| `POST /api/plm/detect` | PLM 转换前的列识别和预览 |
| `POST /api/plm/convert` | 转换为 PLM 导入格式 |
| `POST /api/plm/spec_extract` | 提取规格型号列表 |
| `POST /api/plm/auto_spec_reverse` | PLM 自动规格反查 |
| `POST /api/plm/auto_hq_attachments` | PLM HQ 料号附件下载 |
| `POST /api/bom_compare/customer_hq_preview` | 客户 BOM 与 HQ BOM 预览 |
| `POST /api/bom_compare/customer_hq_export` | 客户 BOM 与 HQ BOM 导出 |
| `POST /api/bom_compare/generic_sheets` | 通用比对读取 Sheet 和表头 |
| `POST /api/bom_compare/generic` | 通用 BOM 比对 |
| `POST /api/bom_compare/local_sheets` | BOM 比对读取本地 Sheet |
| `POST /api/bom_compare/hq_version` | HQ BOM 版本比对 |
| `POST /api/bom_compare/machine_hq_version` | 整机 HQ BOM 版本比对 |
| `GET /api/manufacturer_aliases` | 查询厂商别名映射 |
| `GET /api/manufacturer_aliases/lookup` | 查询单个厂商名映射 |
| `POST /api/manufacturer_aliases` | 新增厂商别名映射 |
| `DELETE /api/manufacturer_aliases/<alias_id>` | 删除厂商别名映射 |
| `GET /api/bug_reports` | 查询 Bug 列表 |
| `POST /api/bug_reports` | 提交 Bug |
| `POST /api/bug_reports/<report_id>/status` | 更新 Bug 状态 |
| `GET /api/feature_requests` | 查询需求列表 |
| `POST /api/feature_requests` | 提交需求 |
| `POST /api/feature_requests/<request_id>/like` | 给需求点赞 |
| `POST /api/feature_requests/<request_id>/status` | 更新需求状态 |

## 21. 后续维护建议

- 对飞书表格 Token、Sheet ID、字段映射建立版本化配置，避免多人浏览器配置不一致。
- 为 BOM 比对、PLM 转换等高风险功能补充更多样例文件和回归测试。
- 将前端内联脚本逐步拆分为模块，便于维护大型页面。
- 对 PLM 自动化功能增加页面变更诊断日志，降低页面改版后的排查成本。
- 对所有生成文件增加统一命名规则，便于用户追踪来源和处理时间。
