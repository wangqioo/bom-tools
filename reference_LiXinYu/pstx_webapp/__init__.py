"""Web application package.

Use ``pstx_webapp.app_factory`` for the Flask app factory and the sibling modules for
state, page rendering, project IO, form parsing, compare view helpers, and
server configuration. This package intentionally avoids old wildcard exports.
"""
